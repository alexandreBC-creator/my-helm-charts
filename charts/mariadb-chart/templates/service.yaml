# Headless service
---
apiVersion: v1
kind: Service
metadata:
  name: mariadb-service-{{ .Values.env }}
  labels:
    app: mariadb
    kind: database
spec:
  ports:
  - port: 3306
    name: mariadb-port
  clusterIP: None
  selector:
    app: mariadb